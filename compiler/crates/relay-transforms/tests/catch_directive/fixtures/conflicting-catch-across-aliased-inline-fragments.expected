==================================== INPUT ====================================
fragment Foo on Node {
    ... on MaybeNodeInterface @alias(as: "aliased_fragment") {
        name @catch
    }
    name
}
==================================== OUTPUT ===================================
fragment Foo on Node {
  ... on MaybeNodeInterface @__FragmentAliasMetadata
  # FragmentAliasMetadata {
  #     alias: WithLocation {
  #         location: conflicting-catch-across-aliased-inline-fragments.graphql:60:62,
  #         item: "aliased_fragment",
  #     },
  #     type_condition: Some(
  #         Interface(0),
  #     ),
  #     selection_type: Interface(0),
  # }
   {
    name @catch @__CatchMetadataDirective
    # CatchMetadataDirective {
    #     to: Result,
    #     path: "aliased_fragment.name",
    # }
    
  }
  name
}
